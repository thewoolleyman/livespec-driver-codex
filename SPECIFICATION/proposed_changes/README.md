# SPECIFICATION/proposed_changes

Pending proposed changes against this spec land here as
`<topic>.md` files, filed by `livespec:propose-change`. The next
`livespec:revise` pass walks this queue, accepting or rejecting each
proposal and then snapshotting the result under `history/vNNN/`.

The queue is currently empty.
