livespec:next

Rank the next spec-side action (revise, propose-change, critique,
prune-history, or none) over the current `SPECIFICATION/proposed_changes/`
and `SPECIFICATION/history/` state, and emit the ranked recommendation.
