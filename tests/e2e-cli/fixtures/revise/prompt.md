livespec:revise

Process the pending proposed changes: accept the review-constraint
proposal, apply its spec edits, and snapshot the result as a new
`SPECIFICATION/history/v002/` revision. Clear the accepted proposal from
`proposed_changes/`.
