livespec:seed

Seed a brand-new livespec specification for a small example project.
Use the `livespec` template and write the main spec to
`SPECIFICATION/spec.md` (two-part path per the seed-payload path
convention). Materialize the `proposed_changes/` and `history/v001/`
scaffolding under `SPECIFICATION/`.
