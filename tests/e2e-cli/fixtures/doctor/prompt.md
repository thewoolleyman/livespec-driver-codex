livespec:doctor

Run the doctor checks against the spec tree: the static phase
(structural invariants) plus the LLM-driven objective and subjective
phases. Surface any findings. The spec tree under `SPECIFICATION/` must
still be intact afterward.
